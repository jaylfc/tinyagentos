id: nemoclaw
name: NemoClaw
type: agent-framework
version: 0.1.0
description: "NVIDIA's enterprise agent framework — GPU-optimised with NeMo integration"
homepage: https://github.com/NVIDIA/nemoclaw
license: Apache-2.0
requires:
  ram_mb: 512
install:
  method: pip
  package: nemoclaw
hardware_tiers:
  arm-npu-16gb: degraded
  x86-cuda-12gb: full
  x86-cuda-24gb: full
  cpu-only: degraded
