export function PlaceholderApp({ windowId: _windowId }: { windowId: string }) {
  return (
    <div className="flex items-center justify-center h-full text-shell-text-secondary">
      <p>App not yet migrated</p>
    </div>
  );
}
